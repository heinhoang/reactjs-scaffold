// Actions bar (Add filter + refresh)
    // Add Filter
    // RefreshButton
        // -> refresh
// Filters bar
    // Filter
        // -> setFilters
        // -> hideFilter
// DataGrid
    // DatagridHeaderCell
        // -> sort
    // DataGridBody
        // Data Grid Cells
// Pagination

// fetch data (resource name, fields, pagination, { filter, default filter })
// change params (resourcen name, query)
